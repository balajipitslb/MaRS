package its.mnr.mp5.vo.invoice;

import its.mnr.mp5.baseclass.ExtendedViewObjectImpl;

import oracle.jbo.RowIterator;
import oracle.jbo.domain.Date;
import oracle.jbo.domain.Number;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Fri Aug 03 16:32:52 PDT 2012
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class MrltInvoiceitemViewImpl extends ExtendedViewObjectImpl {
    public boolean createInvoiceItem(Number pItemId, Number pHeaderId,String pWO, String pEquip, String pVer,
                                     Double pLabor, Double pPart, Double pTax, Double pTotal,Date pCompdt, Date pStartdt,
                                     String pHdrCode){
        //System.out.println("inside MrltInvoiceitemViewImpl:createInvoiceItem()");
        Boolean ret = true;
        //DBTransaction trans = getDBTransaction();
        //Number ntrlacd = getValueFromSequence("S5ACCOUNTDETAIL", trans); 
        
        RowIterator iter = this.getViewObject();
        MrltInvoiceitemViewRowImpl rowImpl = (MrltInvoiceitemViewRowImpl)iter.createRow();
        iter.insertRow(rowImpl);
        
        rowImpl.setInvoiceitemid(pItemId);
        rowImpl.setInvoiceheaderid(pHeaderId);
        rowImpl.setWo(pWO);
        rowImpl.setEquip(pEquip);
        rowImpl.setInvver(pVer);
        rowImpl.setLabor(pLabor);
        rowImpl.setPart(pPart);
        rowImpl.setTax(pTax);
        rowImpl.setTotal(pTotal);
        rowImpl.setCompletedt(pCompdt);
        rowImpl.setStartdt(pStartdt);
        rowImpl.setHdrcode(pHdrCode);
        
        return ret;
        }
    /**
     * This is the default constructor (do not remove).
     */
    public MrltInvoiceitemViewImpl() {
    }

    /**
     * Returns the variable value for headerIdBind.
     * @return variable value for headerIdBind
     */
    public Number getheaderIdBind() {
        return (Number)ensureVariableManager().getVariableValue("headerIdBind");
    }

    /**
     * Sets <code>value</code> for variable headerIdBind.
     * @param value value to bind as headerIdBind
     */
    public void setheaderIdBind(Number value) {
        ensureVariableManager().setVariableValue("headerIdBind", value);
    }
}
